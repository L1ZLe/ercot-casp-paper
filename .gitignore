# LaTeX build artifacts
*.aux
*.bbl
*.blg
*.log
*.out

# Built paper PDF (source lives in the paper folder's paper.tex)
AIstats research paper/paper.pdf

# Python
__pycache__/
*.pyc
.venv/
.ruff_cache/

# Derived experiment results. The current canonical summary artifacts are
# committed for collaborators (ADR-0012); per-seed arrays, logs, checkpoints,
# and the stale 2-seed run stay ignored.
code/results/*
!code/results/results.json
!code/results/main_conditions.json
!code/results/cross_year_results.json
!code/results/calendar_oov_results.json
!code/results/monthly_results.json
!code/results/probe_pairs_5seed.json
!code/results/sensitivity_results.json
!code/results/attention_analysis.json
!code/results/README.md
*.npy
chart_tmp/

# Trained model checkpoints — single home in models/; regenerable by training
# (policy in ADR-0002, TODO-2). The old root-level best_model_*.pth artifacts
# were consolidated into models/ 2026-09-13.
models/

# Local editor settings
.vscode/

# OS
.DS_Store

# Windows NTFS alternate-data-stream metadata (never real project content)
*:Zone.Identifier
*Zone.Identifier

# Regenerable experiment outputs (root-level, from run_*.py / main.py)

!code/ercot_checksums.json
!manifest.json
!sanitization_report.json
!verification_report.json
